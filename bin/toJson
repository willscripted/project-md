#!/usr/bin/env bash

# In: markdown
# Out: project json

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

# '[ ] task\n' => '[ ] task\n\n'
task_correct='s/\(\[.\] .*\)/- \1\n/g'

cat \
 | sed "$task_correct" \
 | pandoc -t json -s \
 | $DIR/../src/toJson.py \
 | cat
